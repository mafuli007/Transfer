package BrazilCenter.transfer.utils;

import org.apache.log4j.Logger;

import BrazilCenter.transfer.Transfer.Main;

public class LogUtils {
	public static Logger logger = Logger.getLogger(Main.conf.getSoftwareId());
}
