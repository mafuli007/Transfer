package BrazilCenter.transfer.core;

import junit.framework.TestCase;

public class FtpTransferTest extends TestCase {

	public void testAddFailedRecord() {
/*		
		XMLOperator operator = new XMLOperator();
		operator.Initial();
		Configuration conf = operator.getConfiguration();
		FtpServerAddress addres = conf.getAddresslist().get(0);
		System.out.println(addres.toString());
		
		Task task = new Task();
		task.setFilename("aa.txxt");
		task.setFilepath("/home/cdd");
		task.setTaskType(TASKTYPE.NewTask);*/
		
/*		FtpTransfer transferThread = new FtpTransfer(conf, null, addres);
		transferThread.addFailedRecord(task);*/
	}
}
